# Ton workflow, mais avec les versions @v4 (ou les plus récentes)
name: Deploy Hugo site to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

defaults:
  run:
    shell: bash

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      HUGO_VERSION: 0.111.3
    steps:
      # 1. Checkout
      - name: Checkout repository
        uses: actions/checkout@v4 # <-- Mis à jour
        with:
          submodules: recursive

      # 2. Installer Hugo CLI
      - name: Install Hugo
        run: |
          wget -O ${{ runner.temp }}/hugo.deb https://github.com/gohugoio/hugo/releases/download/v${HUGO_VERSION}/hugo_extended_${HUGO_VERSION}_linux-amd64.deb
          sudo dpkg -i ${{ runner.temp }}/hugo.deb

      # 3. Installer Dart Sass
      - name: Install Dart Sass
        run: sudo snap install dart-sass

      # 4. Installer Node.js dependencies
      - name: Install Node.js dependencies
        run: "[[ -f package-lock.json || -f npm-shrinkwrap.json ]] && npm ci || true"

      # 5. Configurer GitHub Pages
      - name: Setup Pages
        id: pages
        uses: actions/configure-pages@v4 # <-- Mis à jour

      # 6. Build Hugo site
      - name: Build Hugo site
        env:
          HUGO_ENVIRONMENT: production
          HUGO_ENV: production
        run: |
          export PATH=$PATH:/snap/bin # <-- AJOUTE CETTE LIGNE
          hugo --minify --baseURL "${{ steps.pages.outputs.base_url }}/"

      # 7. Upload du site
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3 # <-- Mis à jour (v3 est la dernière ici)
        with:
          path: ./public

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      # 8. Déploiement
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 # <-- Mis à jour

